{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "hello world",
  "scripts": {
    "start": "nodemon --watch main.js --exec npm run build",
    "build": "electron ."
  },
  "main": "main.js",
  "license": "MIT",
  "devDependencies": {
    "electron": "^23.1.3"
  }
}
